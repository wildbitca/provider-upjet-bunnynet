# Go
vendor/
.work/
_output/

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.xpkg
.agent-output/
.work/
.cache/

# Monolith provider main is generated but not shipped by this repo; keep it out
# of version control so `make check-diff` stays clean after `make generate`.
cmd/provider/monolith/
