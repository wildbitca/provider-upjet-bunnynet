---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: pullzones.cdn.upjet-bunnynet.m.upbound.io
spec:
  group: cdn.upjet-bunnynet.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - upjet-bunnynet
    kind: Pullzone
    listKind: PullzoneList
    plural: pullzones
    singular: pullzone
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Pullzone is the Schema for the Pullzones API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PullzoneSpec defines the desired state of Pullzone
            properties:
              forProvider:
                properties:
                  addCanonicalHeader:
                    description: Indicates whether the Canonical header is added to
                      the responses.
                    type: boolean
                  allowReferers:
                    description: 'The list of referrer hostnames that are allowed
                      to access the pull zone. Requests containing the header "Referer:
                      hostname" that is not on the list will be rejected. If empty,
                      all the referrers are allowed.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockIps:
                    description: The list of IPs that are blocked from accessing the
                      pull zone. Requests coming from the following IPs will be rejected.
                      If empty, all the IPs will be allowed
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockNoReferer:
                    description: Indicates whether requests without a referer should
                      be blocked.
                    type: boolean
                  blockPostRequests:
                    description: Indicates whether to block POST requests.
                    type: boolean
                  blockReferers:
                    description: 'The list of referrer hostnames that are blocked
                      to access the pull zone. Requests containing the header "Referer:
                      hostname" that is not on the list will be rejected. If empty,
                      all the referrers are allowed.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockRootPath:
                    description: This property indicates whether to block the root
                      path.
                    type: boolean
                  cacheChunked:
                    description: Indicates whether the cache slice (Optimize for large
                      object delivery) feature is enabled for the Pull Zone
                    type: boolean
                  cacheEnabled:
                    description: Indicates whether smart caching is enabled.
                    type: boolean
                  cacheErrors:
                    description: Indicates whether bunny.net should be caching error
                      responses.
                    type: boolean
                  cacheExpirationTime:
                    description: The override cache time, in seconds.
                    type: number
                  cacheExpirationTimeBrowser:
                    description: The override cache time for the end client, in seconds.
                    type: number
                  cacheStale:
                    description: 'Options: `offline`, `updating`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVary:
                    description: 'Options: `avif`, `cookie`, `country`, `hostname`,
                      `mobile`, `querystring`, `state`, `webp`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryCookie:
                    description: Contains the list of vary parameters that will be
                      used for vary cache by cookie string. If empty, cookie vary
                      will not be used.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryHeaders:
                    description: Contains the list of request headers will be used
                      for vary cache. If empty, it will not be used.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryQuerystring:
                    description: Contains the list of vary parameters that will be
                      used for vary cache by query string. If empty, all parameters
                      will be used to construct the key
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  corsEnabled:
                    description: Indicates whether CORS (Cross-Origin Resource Sharing)
                      is enabled.
                    type: boolean
                  corsExtensions:
                    description: A list of file extensions for which CORS is enabled.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  disableLetsencrypt:
                    description: If true, the built-in let's encrypt is disabled and
                      requests are passed to the origin.
                    type: boolean
                  errorpageCustomContent:
                    description: Contains the custom error page code that will be
                      returned.
                    type: string
                  errorpageCustomEnabled:
                    description: Indicates whether custom error page code should be
                      enabled.
                    type: boolean
                  errorpageStatuspageCode:
                    description: The statuspage code that will be used to build the
                      status widget.
                    type: string
                  errorpageStatuspageEnabled:
                    description: Indicates whether the statuspage widget should be
                      displayed on the error pages.
                    type: boolean
                  errorpageWhitelabel:
                    description: Indicates whether the error pages should be white-labelled
                      or not
                    type: boolean
                  limitAfter:
                    description: The amount of data after the rate limit will be activated.
                    type: number
                  limitBandwidth:
                    description: The maximum bandwidth limit in bytes.
                    type: number
                  limitBurst:
                    description: Excessive requests are delayed until their number
                      exceeds the maximum burst size.
                    type: number
                  limitConnections:
                    description: The number of connections limited per IP.
                    type: number
                  limitDownloadSpeed:
                    description: The maximum download speed, in kb/s. Use 0 for unlimited.
                    type: number
                  limitRequests:
                    description: The maximum amount of requests per IP per second.
                    type: number
                  logAnonymized:
                    description: Indicates whether logs are anonymized.
                    type: boolean
                  logAnonymizedStyle:
                    description: 'Options: `Drop`, `OneDigit`'
                    type: string
                  logEnabled:
                    description: Indicates whether logging is enabled.
                    type: boolean
                  logForwardEnabled:
                    description: Indicates whether log forwarding is enabled.
                    type: boolean
                  logForwardFormat:
                    description: 'Options: `JSON`, `Plain`'
                    type: string
                  logForwardPort:
                    description: The port number for log forwarding.
                    type: number
                  logForwardProtocol:
                    description: 'Options: `DataDog`, `TCP`, `TCPEncrypted`, `UDP`'
                    type: string
                  logForwardServer:
                    description: The server address for log forwarding.
                    type: string
                  logForwardToken:
                    description: The token used for log forwarding authentication.
                    type: string
                  logStorageEnabled:
                    description: Indicates whether log storage is enabled.
                    type: boolean
                  logStorageZone:
                    description: The storage zone ID for log storage.
                    type: number
                  name:
                    description: The name of the pull zone.
                    type: string
                  optimizerBurrow:
                    description: Indicates whether Burrow Smart Routing should be
                      enabled.
                    type: boolean
                  optimizerClassesForce:
                    description: Indicates whether the optimizer class list should
                      be enforced.
                    type: boolean
                  optimizerDynamicImageApi:
                    description: Indicates whether the image manipulation should be
                      enabled.
                    type: boolean
                  optimizerEnabled:
                    description: Indicates whether Bunny Optimizer should be enabled.
                    type: boolean
                  optimizerHtmlPrerender:
                    description: Indicates whether HTML Prerender should be enabled.
                    type: boolean
                  optimizerMinifyCss:
                    description: Indicates whether the CSS minification should be
                      enabled.
                    type: boolean
                  optimizerMinifyJs:
                    description: Indicates whether the JavaScript minification should
                      be enabled.
                    type: boolean
                  optimizerSmartimage:
                    description: Indicates whether the automatic image optimization
                      should be enabled.
                    type: boolean
                  optimizerSmartimageDesktopMaxwidth:
                    description: The maximum automatic image size for desktop clients.
                    type: number
                  optimizerSmartimageDesktopQuality:
                    description: The image quality for desktop clients.
                    type: number
                  optimizerSmartimageMobileMaxwidth:
                    description: The maximum automatic image size for mobile clients.
                    type: number
                  optimizerSmartimageMobileQuality:
                    description: Determines the image quality for mobile clients
                    type: number
                  optimizerWatermark:
                    description: Indicates whether image watermarking should be enabled.
                    type: boolean
                  optimizerWatermarkBorderoffset:
                    description: The offset of the watermark image.
                    type: number
                  optimizerWatermarkMinsize:
                    description: The minimum image size to which the watermark will
                      be added.
                    type: number
                  optimizerWatermarkPosition:
                    description: 'Options: `BottomLeft`, `BottomRight`, `Center`,
                      `CenterStretch`, `TopLeft`, `TopRight`'
                    type: string
                  optimizerWatermarkUrl:
                    description: The URL of the watermark image.
                    type: string
                  optimizerWebp:
                    description: Indicates whether the WebP optimization should be
                      enabled.
                    type: boolean
                  originshieldConcurrencyLimit:
                    description: Indicates whether there is a concurrency limit for
                      Origin Shield.
                    type: boolean
                  originshieldConcurrencyRequests:
                    description: The number of concurrent requests for Origin Shield.
                    type: number
                  originshieldEnabled:
                    description: Indicates whether Origin Shield is enabled.
                    type: boolean
                  originshieldQueueRequests:
                    description: The number of queued requests for Origin Shield.
                    type: number
                  originshieldQueueWait:
                    description: The maximum wait time for queued requests in Origin
                      Shield, in seconds.
                    type: number
                  originshieldZone:
                    description: 'Options: `FR`, `IL`'
                    type: string
                  permacacheStoragezone:
                    description: The storage zone ID for Perma-Cache.
                    type: number
                  requestCoalescingEnabled:
                    description: Indicates whether request coalescing is enabled.
                    type: boolean
                  requestCoalescingTimeout:
                    description: Specifies the timeout period, in seconds, for request
                      coalescing, determining how long to wait before sending combined
                      requests to the origin.
                    type: number
                  routing:
                    items:
                      properties:
                        blockedCountries:
                          description: The list of blocked countries with the two-letter
                            Alpha2 ISO codes. Traffic connecting from a blocked country
                            will be rejected on the DNS level.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        filters:
                          description: 'Options: `all`, `eu`, `scripting`'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        redirectedCountries:
                          description: The list of budget redirected countries with
                            the two-letter Alpha2 ISO codes. Traffic from a redirected
                            country will connect to the cheapest possible node in
                            North America or Europe.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        tier:
                          description: 'Options: `Standard`, `Volume`'
                          type: string
                        zones:
                          description: 'Options: `AF`, `ASIA`, `EU`, `SA`, `US`'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  s3AuthEnabled:
                    description: Indicates whether requests to origin will be signed
                      with AWS Signature Version 4.
                    type: boolean
                  s3AuthKey:
                    description: The access key used to authenticate the requests.
                    type: string
                  s3AuthRegion:
                    description: The region name of the bucket used to authenticate
                      the requests.
                    type: string
                  s3AuthSecret:
                    description: The secret key used to authenticate the requests.
                    type: string
                  safehopConnectionTimeout:
                    description: The amount of seconds to wait when connecting to
                      the origin. Otherwise the request will fail or retry.
                    type: number
                  safehopEnabled:
                    type: boolean
                  safehopResponseTimeout:
                    description: The amount of seconds to wait when waiting for the
                      origin reply. Otherwise the request will fail or retry.
                    type: number
                  safehopRetryCount:
                    description: The number of retries to the origin server.
                    type: number
                  safehopRetryDelay:
                    description: The amount of time that the CDN should wait before
                      retrying an origin request.
                    type: number
                  safehopRetryReasons:
                    description: 'Options: `5xxResponse`, `connectionTimeout`, `responseTimeout`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  sortQuerystring:
                    description: If enabled, the query parameters will be automatically
                      sorted into a consistent order before checking the cache.
                    type: boolean
                  stripCookies:
                    description: If enabled, bunny.net will strip all the Set-Cookie
                      headers from the HTTP responses.
                    type: boolean
                  tlsSupport:
                    description: 'Options: `TLSv1.0`, `TLSv1.1`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  tokenAuthEnabled:
                    description: Indicates whether requests without a valid token
                      and expiry timestamp will be rejected.
                    type: boolean
                  tokenAuthIpValidation:
                    description: Indicates whether the secure hash generated by the
                      server will also include an IP address.
                    type: boolean
                  useBackgroundUpdate:
                    description: Indicates whether cache update is performed in the
                      background.
                    type: boolean
                  websocketsEnabled:
                    description: Indicates whether the WebSocket support is enabled.
                    type: boolean
                  websocketsMaxConnections:
                    description: The maximum allowed concurrent WebSocket connections.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addCanonicalHeader:
                    description: Indicates whether the Canonical header is added to
                      the responses.
                    type: boolean
                  allowReferers:
                    description: 'The list of referrer hostnames that are allowed
                      to access the pull zone. Requests containing the header "Referer:
                      hostname" that is not on the list will be rejected. If empty,
                      all the referrers are allowed.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockIps:
                    description: The list of IPs that are blocked from accessing the
                      pull zone. Requests coming from the following IPs will be rejected.
                      If empty, all the IPs will be allowed
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockNoReferer:
                    description: Indicates whether requests without a referer should
                      be blocked.
                    type: boolean
                  blockPostRequests:
                    description: Indicates whether to block POST requests.
                    type: boolean
                  blockReferers:
                    description: 'The list of referrer hostnames that are blocked
                      to access the pull zone. Requests containing the header "Referer:
                      hostname" that is not on the list will be rejected. If empty,
                      all the referrers are allowed.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockRootPath:
                    description: This property indicates whether to block the root
                      path.
                    type: boolean
                  cacheChunked:
                    description: Indicates whether the cache slice (Optimize for large
                      object delivery) feature is enabled for the Pull Zone
                    type: boolean
                  cacheEnabled:
                    description: Indicates whether smart caching is enabled.
                    type: boolean
                  cacheErrors:
                    description: Indicates whether bunny.net should be caching error
                      responses.
                    type: boolean
                  cacheExpirationTime:
                    description: The override cache time, in seconds.
                    type: number
                  cacheExpirationTimeBrowser:
                    description: The override cache time for the end client, in seconds.
                    type: number
                  cacheStale:
                    description: 'Options: `offline`, `updating`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVary:
                    description: 'Options: `avif`, `cookie`, `country`, `hostname`,
                      `mobile`, `querystring`, `state`, `webp`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryCookie:
                    description: Contains the list of vary parameters that will be
                      used for vary cache by cookie string. If empty, cookie vary
                      will not be used.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryHeaders:
                    description: Contains the list of request headers will be used
                      for vary cache. If empty, it will not be used.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryQuerystring:
                    description: Contains the list of vary parameters that will be
                      used for vary cache by query string. If empty, all parameters
                      will be used to construct the key
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  corsEnabled:
                    description: Indicates whether CORS (Cross-Origin Resource Sharing)
                      is enabled.
                    type: boolean
                  corsExtensions:
                    description: A list of file extensions for which CORS is enabled.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  disableLetsencrypt:
                    description: If true, the built-in let's encrypt is disabled and
                      requests are passed to the origin.
                    type: boolean
                  errorpageCustomContent:
                    description: Contains the custom error page code that will be
                      returned.
                    type: string
                  errorpageCustomEnabled:
                    description: Indicates whether custom error page code should be
                      enabled.
                    type: boolean
                  errorpageStatuspageCode:
                    description: The statuspage code that will be used to build the
                      status widget.
                    type: string
                  errorpageStatuspageEnabled:
                    description: Indicates whether the statuspage widget should be
                      displayed on the error pages.
                    type: boolean
                  errorpageWhitelabel:
                    description: Indicates whether the error pages should be white-labelled
                      or not
                    type: boolean
                  limitAfter:
                    description: The amount of data after the rate limit will be activated.
                    type: number
                  limitBandwidth:
                    description: The maximum bandwidth limit in bytes.
                    type: number
                  limitBurst:
                    description: Excessive requests are delayed until their number
                      exceeds the maximum burst size.
                    type: number
                  limitConnections:
                    description: The number of connections limited per IP.
                    type: number
                  limitDownloadSpeed:
                    description: The maximum download speed, in kb/s. Use 0 for unlimited.
                    type: number
                  limitRequests:
                    description: The maximum amount of requests per IP per second.
                    type: number
                  logAnonymized:
                    description: Indicates whether logs are anonymized.
                    type: boolean
                  logAnonymizedStyle:
                    description: 'Options: `Drop`, `OneDigit`'
                    type: string
                  logEnabled:
                    description: Indicates whether logging is enabled.
                    type: boolean
                  logForwardEnabled:
                    description: Indicates whether log forwarding is enabled.
                    type: boolean
                  logForwardFormat:
                    description: 'Options: `JSON`, `Plain`'
                    type: string
                  logForwardPort:
                    description: The port number for log forwarding.
                    type: number
                  logForwardProtocol:
                    description: 'Options: `DataDog`, `TCP`, `TCPEncrypted`, `UDP`'
                    type: string
                  logForwardServer:
                    description: The server address for log forwarding.
                    type: string
                  logForwardToken:
                    description: The token used for log forwarding authentication.
                    type: string
                  logStorageEnabled:
                    description: Indicates whether log storage is enabled.
                    type: boolean
                  logStorageZone:
                    description: The storage zone ID for log storage.
                    type: number
                  name:
                    description: The name of the pull zone.
                    type: string
                  optimizerBurrow:
                    description: Indicates whether Burrow Smart Routing should be
                      enabled.
                    type: boolean
                  optimizerClassesForce:
                    description: Indicates whether the optimizer class list should
                      be enforced.
                    type: boolean
                  optimizerDynamicImageApi:
                    description: Indicates whether the image manipulation should be
                      enabled.
                    type: boolean
                  optimizerEnabled:
                    description: Indicates whether Bunny Optimizer should be enabled.
                    type: boolean
                  optimizerHtmlPrerender:
                    description: Indicates whether HTML Prerender should be enabled.
                    type: boolean
                  optimizerMinifyCss:
                    description: Indicates whether the CSS minification should be
                      enabled.
                    type: boolean
                  optimizerMinifyJs:
                    description: Indicates whether the JavaScript minification should
                      be enabled.
                    type: boolean
                  optimizerSmartimage:
                    description: Indicates whether the automatic image optimization
                      should be enabled.
                    type: boolean
                  optimizerSmartimageDesktopMaxwidth:
                    description: The maximum automatic image size for desktop clients.
                    type: number
                  optimizerSmartimageDesktopQuality:
                    description: The image quality for desktop clients.
                    type: number
                  optimizerSmartimageMobileMaxwidth:
                    description: The maximum automatic image size for mobile clients.
                    type: number
                  optimizerSmartimageMobileQuality:
                    description: Determines the image quality for mobile clients
                    type: number
                  optimizerWatermark:
                    description: Indicates whether image watermarking should be enabled.
                    type: boolean
                  optimizerWatermarkBorderoffset:
                    description: The offset of the watermark image.
                    type: number
                  optimizerWatermarkMinsize:
                    description: The minimum image size to which the watermark will
                      be added.
                    type: number
                  optimizerWatermarkPosition:
                    description: 'Options: `BottomLeft`, `BottomRight`, `Center`,
                      `CenterStretch`, `TopLeft`, `TopRight`'
                    type: string
                  optimizerWatermarkUrl:
                    description: The URL of the watermark image.
                    type: string
                  optimizerWebp:
                    description: Indicates whether the WebP optimization should be
                      enabled.
                    type: boolean
                  originshieldConcurrencyLimit:
                    description: Indicates whether there is a concurrency limit for
                      Origin Shield.
                    type: boolean
                  originshieldConcurrencyRequests:
                    description: The number of concurrent requests for Origin Shield.
                    type: number
                  originshieldEnabled:
                    description: Indicates whether Origin Shield is enabled.
                    type: boolean
                  originshieldQueueRequests:
                    description: The number of queued requests for Origin Shield.
                    type: number
                  originshieldQueueWait:
                    description: The maximum wait time for queued requests in Origin
                      Shield, in seconds.
                    type: number
                  originshieldZone:
                    description: 'Options: `FR`, `IL`'
                    type: string
                  permacacheStoragezone:
                    description: The storage zone ID for Perma-Cache.
                    type: number
                  requestCoalescingEnabled:
                    description: Indicates whether request coalescing is enabled.
                    type: boolean
                  requestCoalescingTimeout:
                    description: Specifies the timeout period, in seconds, for request
                      coalescing, determining how long to wait before sending combined
                      requests to the origin.
                    type: number
                  routing:
                    items:
                      properties:
                        blockedCountries:
                          description: The list of blocked countries with the two-letter
                            Alpha2 ISO codes. Traffic connecting from a blocked country
                            will be rejected on the DNS level.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        filters:
                          description: 'Options: `all`, `eu`, `scripting`'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        redirectedCountries:
                          description: The list of budget redirected countries with
                            the two-letter Alpha2 ISO codes. Traffic from a redirected
                            country will connect to the cheapest possible node in
                            North America or Europe.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        tier:
                          description: 'Options: `Standard`, `Volume`'
                          type: string
                        zones:
                          description: 'Options: `AF`, `ASIA`, `EU`, `SA`, `US`'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  s3AuthEnabled:
                    description: Indicates whether requests to origin will be signed
                      with AWS Signature Version 4.
                    type: boolean
                  s3AuthKey:
                    description: The access key used to authenticate the requests.
                    type: string
                  s3AuthRegion:
                    description: The region name of the bucket used to authenticate
                      the requests.
                    type: string
                  s3AuthSecret:
                    description: The secret key used to authenticate the requests.
                    type: string
                  safehopConnectionTimeout:
                    description: The amount of seconds to wait when connecting to
                      the origin. Otherwise the request will fail or retry.
                    type: number
                  safehopEnabled:
                    type: boolean
                  safehopResponseTimeout:
                    description: The amount of seconds to wait when waiting for the
                      origin reply. Otherwise the request will fail or retry.
                    type: number
                  safehopRetryCount:
                    description: The number of retries to the origin server.
                    type: number
                  safehopRetryDelay:
                    description: The amount of time that the CDN should wait before
                      retrying an origin request.
                    type: number
                  safehopRetryReasons:
                    description: 'Options: `5xxResponse`, `connectionTimeout`, `responseTimeout`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  sortQuerystring:
                    description: If enabled, the query parameters will be automatically
                      sorted into a consistent order before checking the cache.
                    type: boolean
                  stripCookies:
                    description: If enabled, bunny.net will strip all the Set-Cookie
                      headers from the HTTP responses.
                    type: boolean
                  tlsSupport:
                    description: 'Options: `TLSv1.0`, `TLSv1.1`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  tokenAuthEnabled:
                    description: Indicates whether requests without a valid token
                      and expiry timestamp will be rejected.
                    type: boolean
                  tokenAuthIpValidation:
                    description: Indicates whether the secure hash generated by the
                      server will also include an IP address.
                    type: boolean
                  useBackgroundUpdate:
                    description: Indicates whether cache update is performed in the
                      background.
                    type: boolean
                  websocketsEnabled:
                    description: Indicates whether the WebSocket support is enabled.
                    type: boolean
                  websocketsMaxConnections:
                    description: The maximum allowed concurrent WebSocket connections.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: PullzoneStatus defines the observed state of Pullzone.
            properties:
              atProvider:
                properties:
                  addCanonicalHeader:
                    description: Indicates whether the Canonical header is added to
                      the responses.
                    type: boolean
                  allowReferers:
                    description: 'The list of referrer hostnames that are allowed
                      to access the pull zone. Requests containing the header "Referer:
                      hostname" that is not on the list will be rejected. If empty,
                      all the referrers are allowed.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockIps:
                    description: The list of IPs that are blocked from accessing the
                      pull zone. Requests coming from the following IPs will be rejected.
                      If empty, all the IPs will be allowed
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockNoReferer:
                    description: Indicates whether requests without a referer should
                      be blocked.
                    type: boolean
                  blockPostRequests:
                    description: Indicates whether to block POST requests.
                    type: boolean
                  blockReferers:
                    description: 'The list of referrer hostnames that are blocked
                      to access the pull zone. Requests containing the header "Referer:
                      hostname" that is not on the list will be rejected. If empty,
                      all the referrers are allowed.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  blockRootPath:
                    description: This property indicates whether to block the root
                      path.
                    type: boolean
                  cacheChunked:
                    description: Indicates whether the cache slice (Optimize for large
                      object delivery) feature is enabled for the Pull Zone
                    type: boolean
                  cacheEnabled:
                    description: Indicates whether smart caching is enabled.
                    type: boolean
                  cacheErrors:
                    description: Indicates whether bunny.net should be caching error
                      responses.
                    type: boolean
                  cacheExpirationTime:
                    description: The override cache time, in seconds.
                    type: number
                  cacheExpirationTimeBrowser:
                    description: The override cache time for the end client, in seconds.
                    type: number
                  cacheStale:
                    description: 'Options: `offline`, `updating`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVary:
                    description: 'Options: `avif`, `cookie`, `country`, `hostname`,
                      `mobile`, `querystring`, `state`, `webp`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryCookie:
                    description: Contains the list of vary parameters that will be
                      used for vary cache by cookie string. If empty, cookie vary
                      will not be used.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryHeaders:
                    description: Contains the list of request headers will be used
                      for vary cache. If empty, it will not be used.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cacheVaryQuerystring:
                    description: Contains the list of vary parameters that will be
                      used for vary cache by query string. If empty, all parameters
                      will be used to construct the key
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  cdnDomain:
                    description: The CNAME domain of the pull zone for setting up
                      custom hostnames
                    type: string
                  corsEnabled:
                    description: Indicates whether CORS (Cross-Origin Resource Sharing)
                      is enabled.
                    type: boolean
                  corsExtensions:
                    description: A list of file extensions for which CORS is enabled.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  disableLetsencrypt:
                    description: If true, the built-in let's encrypt is disabled and
                      requests are passed to the origin.
                    type: boolean
                  errorpageCustomContent:
                    description: Contains the custom error page code that will be
                      returned.
                    type: string
                  errorpageCustomEnabled:
                    description: Indicates whether custom error page code should be
                      enabled.
                    type: boolean
                  errorpageStatuspageCode:
                    description: The statuspage code that will be used to build the
                      status widget.
                    type: string
                  errorpageStatuspageEnabled:
                    description: Indicates whether the statuspage widget should be
                      displayed on the error pages.
                    type: boolean
                  errorpageWhitelabel:
                    description: Indicates whether the error pages should be white-labelled
                      or not
                    type: boolean
                  id:
                    type: string
                  limitAfter:
                    description: The amount of data after the rate limit will be activated.
                    type: number
                  limitBandwidth:
                    description: The maximum bandwidth limit in bytes.
                    type: number
                  limitBurst:
                    description: Excessive requests are delayed until their number
                      exceeds the maximum burst size.
                    type: number
                  limitConnections:
                    description: The number of connections limited per IP.
                    type: number
                  limitDownloadSpeed:
                    description: The maximum download speed, in kb/s. Use 0 for unlimited.
                    type: number
                  limitRequests:
                    description: The maximum amount of requests per IP per second.
                    type: number
                  logAnonymized:
                    description: Indicates whether logs are anonymized.
                    type: boolean
                  logAnonymizedStyle:
                    description: 'Options: `Drop`, `OneDigit`'
                    type: string
                  logEnabled:
                    description: Indicates whether logging is enabled.
                    type: boolean
                  logForwardEnabled:
                    description: Indicates whether log forwarding is enabled.
                    type: boolean
                  logForwardFormat:
                    description: 'Options: `JSON`, `Plain`'
                    type: string
                  logForwardPort:
                    description: The port number for log forwarding.
                    type: number
                  logForwardProtocol:
                    description: 'Options: `DataDog`, `TCP`, `TCPEncrypted`, `UDP`'
                    type: string
                  logForwardServer:
                    description: The server address for log forwarding.
                    type: string
                  logForwardToken:
                    description: The token used for log forwarding authentication.
                    type: string
                  logStorageEnabled:
                    description: Indicates whether log storage is enabled.
                    type: boolean
                  logStorageZone:
                    description: The storage zone ID for log storage.
                    type: number
                  name:
                    description: The name of the pull zone.
                    type: string
                  optimizerBurrow:
                    description: Indicates whether Burrow Smart Routing should be
                      enabled.
                    type: boolean
                  optimizerClassesForce:
                    description: Indicates whether the optimizer class list should
                      be enforced.
                    type: boolean
                  optimizerDynamicImageApi:
                    description: Indicates whether the image manipulation should be
                      enabled.
                    type: boolean
                  optimizerEnabled:
                    description: Indicates whether Bunny Optimizer should be enabled.
                    type: boolean
                  optimizerHtmlPrerender:
                    description: Indicates whether HTML Prerender should be enabled.
                    type: boolean
                  optimizerMinifyCss:
                    description: Indicates whether the CSS minification should be
                      enabled.
                    type: boolean
                  optimizerMinifyJs:
                    description: Indicates whether the JavaScript minification should
                      be enabled.
                    type: boolean
                  optimizerSmartimage:
                    description: Indicates whether the automatic image optimization
                      should be enabled.
                    type: boolean
                  optimizerSmartimageDesktopMaxwidth:
                    description: The maximum automatic image size for desktop clients.
                    type: number
                  optimizerSmartimageDesktopQuality:
                    description: The image quality for desktop clients.
                    type: number
                  optimizerSmartimageMobileMaxwidth:
                    description: The maximum automatic image size for mobile clients.
                    type: number
                  optimizerSmartimageMobileQuality:
                    description: Determines the image quality for mobile clients
                    type: number
                  optimizerWatermark:
                    description: Indicates whether image watermarking should be enabled.
                    type: boolean
                  optimizerWatermarkBorderoffset:
                    description: The offset of the watermark image.
                    type: number
                  optimizerWatermarkMinsize:
                    description: The minimum image size to which the watermark will
                      be added.
                    type: number
                  optimizerWatermarkPosition:
                    description: 'Options: `BottomLeft`, `BottomRight`, `Center`,
                      `CenterStretch`, `TopLeft`, `TopRight`'
                    type: string
                  optimizerWatermarkUrl:
                    description: The URL of the watermark image.
                    type: string
                  optimizerWebp:
                    description: Indicates whether the WebP optimization should be
                      enabled.
                    type: boolean
                  origin:
                    items:
                      properties:
                        containerAppId:
                          description: The ID if the compute container app.
                          type: string
                        containerEndpointId:
                          description: The ID if the compute container app endpoint.
                          type: string
                        dnsPort:
                          description: The port for DNS Accelerated endpoints.
                          type: number
                        dnsScheme:
                          description: The scheme for DNS Accelerated endpoints.
                          type: string
                        followRedirects:
                          description: Indicates whether the zone will follow origin
                            redirects.
                          type: boolean
                        forwardHostHeader:
                          description: Indicates whether the current hostname is forwarded
                            to the origin.
                          type: boolean
                        hostHeader:
                          description: The host header that will be sent to the origin.
                          type: string
                        middlewareScript:
                          description: The ID of the compute script used as a middleware.
                          type: number
                        script:
                          description: The ID of the linked compute script.
                          type: number
                        storagezone:
                          description: The ID of the linked storage zone.
                          type: number
                        type:
                          description: 'Options: `ComputeContainer`, `ComputeScript`,
                            `DnsAccelerate`, `OriginUrl`, `StorageZone`'
                          type: string
                        url:
                          description: The origin URL from where the files are fetched.
                          type: string
                        verifySsl:
                          description: Indicates whether the Origin's TLS certificate
                            should be verified.
                          type: boolean
                      type: object
                    type: array
                  originshieldConcurrencyLimit:
                    description: Indicates whether there is a concurrency limit for
                      Origin Shield.
                    type: boolean
                  originshieldConcurrencyRequests:
                    description: The number of concurrent requests for Origin Shield.
                    type: number
                  originshieldEnabled:
                    description: Indicates whether Origin Shield is enabled.
                    type: boolean
                  originshieldQueueRequests:
                    description: The number of queued requests for Origin Shield.
                    type: number
                  originshieldQueueWait:
                    description: The maximum wait time for queued requests in Origin
                      Shield, in seconds.
                    type: number
                  originshieldZone:
                    description: 'Options: `FR`, `IL`'
                    type: string
                  permacacheStoragezone:
                    description: The storage zone ID for Perma-Cache.
                    type: number
                  requestCoalescingEnabled:
                    description: Indicates whether request coalescing is enabled.
                    type: boolean
                  requestCoalescingTimeout:
                    description: Specifies the timeout period, in seconds, for request
                      coalescing, determining how long to wait before sending combined
                      requests to the origin.
                    type: number
                  routing:
                    items:
                      properties:
                        blockedCountries:
                          description: The list of blocked countries with the two-letter
                            Alpha2 ISO codes. Traffic connecting from a blocked country
                            will be rejected on the DNS level.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        filters:
                          description: 'Options: `all`, `eu`, `scripting`'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        redirectedCountries:
                          description: The list of budget redirected countries with
                            the two-letter Alpha2 ISO codes. Traffic from a redirected
                            country will connect to the cheapest possible node in
                            North America or Europe.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        tier:
                          description: 'Options: `Standard`, `Volume`'
                          type: string
                        zones:
                          description: 'Options: `AF`, `ASIA`, `EU`, `SA`, `US`'
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  s3AuthEnabled:
                    description: Indicates whether requests to origin will be signed
                      with AWS Signature Version 4.
                    type: boolean
                  s3AuthKey:
                    description: The access key used to authenticate the requests.
                    type: string
                  s3AuthRegion:
                    description: The region name of the bucket used to authenticate
                      the requests.
                    type: string
                  s3AuthSecret:
                    description: The secret key used to authenticate the requests.
                    type: string
                  safehopConnectionTimeout:
                    description: The amount of seconds to wait when connecting to
                      the origin. Otherwise the request will fail or retry.
                    type: number
                  safehopEnabled:
                    type: boolean
                  safehopResponseTimeout:
                    description: The amount of seconds to wait when waiting for the
                      origin reply. Otherwise the request will fail or retry.
                    type: number
                  safehopRetryCount:
                    description: The number of retries to the origin server.
                    type: number
                  safehopRetryDelay:
                    description: The amount of time that the CDN should wait before
                      retrying an origin request.
                    type: number
                  safehopRetryReasons:
                    description: 'Options: `5xxResponse`, `connectionTimeout`, `responseTimeout`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  sortQuerystring:
                    description: If enabled, the query parameters will be automatically
                      sorted into a consistent order before checking the cache.
                    type: boolean
                  stripCookies:
                    description: If enabled, bunny.net will strip all the Set-Cookie
                      headers from the HTTP responses.
                    type: boolean
                  tlsSupport:
                    description: 'Options: `TLSv1.0`, `TLSv1.1`'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  tokenAuthEnabled:
                    description: Indicates whether requests without a valid token
                      and expiry timestamp will be rejected.
                    type: boolean
                  tokenAuthIpValidation:
                    description: Indicates whether the secure hash generated by the
                      server will also include an IP address.
                    type: boolean
                  useBackgroundUpdate:
                    description: Indicates whether cache update is performed in the
                      background.
                    type: boolean
                  websocketsEnabled:
                    description: Indicates whether the WebSocket support is enabled.
                    type: boolean
                  websocketsMaxConnections:
                    description: The maximum allowed concurrent WebSocket connections.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
