apiVersion: meta.pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-bunnynet
  annotations:
    meta.crossplane.io/maintainer: "Wildbit"
    meta.crossplane.io/source: "https://github.com/wildbitca/provider-upjet-bunnynet"
    meta.crossplane.io/license: "Apache-2.0"
    meta.crossplane.io/description: "Upjet-based Crossplane provider for the Bunny.net API (CDN, DNS, Storage, Stream, Compute)."
    meta.crossplane.io/readme: |
      provider-bunnynet exposes XRM-conformant managed resources for the Bunny.net API.
      See the source repository for full documentation, ProviderConfig setup, and supported resources.
spec:
  capabilities:
    - SafeStart
